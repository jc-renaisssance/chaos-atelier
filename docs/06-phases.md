# Phases + open questions

## Phase-1 (locked intent)
**One loop only:** client → craft → sim report → shop.

Must exist before content flood:
1. Tag **catalog schema** + one **resolver**
2. Headless **client × threat × build** report harness
3. Spend gates: gen budget + no gens on locked icons
4. Pass look = overall adventure trend; stamp which tags/stats fired

## Deferred until Phase-1 holds
- Boss ladder depth
- Encyclopedia full illustrations
- Named unique recipes as the default content path
- Parallel special-client lanes
- Deep shop meta / scars systems

## Open questions
- Final product name (Chaos Atelier is temporary)
- Exact starter tag list and thresholds
- Adventure sim resolution rules (combat vs environment checks)
- Shop economy numbers
- IAP thesis (Finance sketches when Jonathan opens that lane)
- Repo default PR target: use `dev` when present (created 2026-09-21)
